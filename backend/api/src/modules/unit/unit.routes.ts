import { unitCreateSchema,unitUpdateSchema } from '@hello-shop/validation'; import type { AuthService } from '../auth/auth.service.js'; import type { AuthRepository } from '../auth/auth.types.js'; import { createMasterRouter } from '../master/master.js'; export const createUnitRouter=(auth:AuthService,repo:AuthRepository)=>createMasterRouter('unit',auth,repo,unitCreateSchema,unitUpdateSchema,'unit');
