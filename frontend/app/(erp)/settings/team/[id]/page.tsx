import {MemberWorkspace} from '@/components/team-security/member-workspaces'; export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;return <MemberWorkspace id={id}/>}
